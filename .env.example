# Convex deployment URL - Get this from running `npx convex dev`
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# Convex deploy key for production - Get this from Convex dashboard
CONVEX_DEPLOY_KEY=your-deploy-key

# BetterAuth - Convex site URL (ends in .site for cloud, or one port higher for self-hosted)
NEXT_PUBLIC_CONVEX_SITE_URL=https://your-project.convex.site

# Your site URL (Next.js client-side)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# IMPORTANT: Convex server-side environment variables (not in .env.local!)
# These must be set via `npx convex env set`:
#
#   npx convex env set SITE_URL http://localhost:3000
#   npx convex env set BETTER_AUTH_SECRET $(openssl rand -base64 32)
#
# For production:
#   npx convex env set SITE_URL https://your-domain.com
#   npx convex env set BETTER_AUTH_SECRET <your-production-secret>

# Google Analytics 4 Measurement ID (production only)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX